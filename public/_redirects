# Cloudflare Pages SPA fallback.
# The Vite build renames index.html -> dashboard.html (the app entry lives at
# /dashboard), so a bare "/" has no index and 404s. Serve dashboard.html for the
# root and any path that isn't a real static asset (assets, data, favico, the
# other HTML entry points like embed.html/settings.html are real files and are
# served directly, taking precedence over this fallback). Mirrors the catch-all
# rewrite in vercel.json.
/*    /dashboard.html    200
